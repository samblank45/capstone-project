@events.map do |event|
  {
    id: event.id,
    title: event.title,
    description: event.description,
    location: event.location,
    user_id: event.user_id,
    date_time: event.date_time
  }
end